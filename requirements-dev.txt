# Development Requirements
# Use this file for development and testing - allows updates to latest versions
# After testing updates, pin the tested version in requirements.txt for production

# Include production requirements as base
-r requirements.txt

# Development overrides - allow testing latest versions
# These will override the pinned versions in requirements.txt
yt-dlp>=2025.9.26  # Test with latest - YouTube API changes frequently

# Development-only packages
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
bandit>=1.7.0
