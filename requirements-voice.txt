# Advanced Voice Fingerprinting Dependencies
# Install with: pip install -r requirements-voice.txt
# NOTE: These are now bundled in the DMG by default

# Core audio processing
librosa>=0.10.0              # Audio feature extraction (MFCC, spectrograms)
soundfile>=0.12.0            # Audio file I/O support for librosa
resampy>=0.4.0               # High-quality audio resampling

# Deep learning for voice embeddings
torch>=2.0.0                 # PyTorch for neural networks
torchaudio>=2.0.0            # Audio processing utilities

# State-of-the-art models
transformers>=4.35.0         # Hugging Face transformers (wav2vec2)
speechbrain>=0.5.0           # ECAPA-TDNN speaker verification models

# Scientific computing
numpy>=1.24.0                # Already in main requirements
scipy>=1.10.0                # Already in main requirements
scikit-learn>=1.3.0          # For cosine similarity and metrics

# Audio utilities
ffmpeg-python>=0.2.0         # FFmpeg integration
webrtcvad>=2.0.10            # Voice activity detection (optional)

# Optional: Additional models and utilities
# speechrecognition>=3.10.0   # Alternative speech recognition
# pyaudio>=0.2.11              # Real-time audio processing
